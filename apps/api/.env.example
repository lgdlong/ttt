# Example environment variables for TTT API (Go Gin backend)
# Copy this file to .env and fill in actual values for production

# --- Core Application ---
GO_ENV=development           # Environment: development | test | production
APP_ENV=development          # Alternative env var (used if GO_ENV is unset)
PORT=8080                    # API server port

# --- JWT Authentication ---
JWT_SECRET=your_jwt_secret_here   # REQUIRED in production! (see auth_service.go)

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:8080/api/auth/google/callback


# --- Database ---
# (Choose either the DB_* variables or DATABASE_URL as needed by your setup)
DB_NAME=ttt_db
DB_USER=ttt_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_SSLMODE=disable
DATABASE_URL=postgres://ttt_user:your_db_password@localhost:5432/ttt_db?sslmode=disable

# --- CORS & Allowed Origins ---
ALLOWED_ORIGIN=http://localhost:3000

# --- YouTube API ---
YOUTUBE_API_KEY=your_youtube_api_key

# --- OpenAI API ---
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=your_openai_api_key

# --- Other (optional, add as needed) ---
# REDIS_URL=redis://localhost:6379/0
# SENTRY_DSN=
# LOG_LEVEL=info
# CORS_ORIGINS=http://localhost:3000
